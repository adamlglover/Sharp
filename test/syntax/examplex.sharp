mod application;

import io;

class X {
    static X yy;

    fn operator+(var x) {

    }
}


class Main base application#X{

    var x = base->yy.yy().y;
    var y=self->foo(Main.x, foo(9)[1].x);
    var yy = base->application#X.foo("foo");
    X[] b;

    fn foo(var x, var n) : var {

    }

    fn foo(var x) : Main[] {

    }

    public static fn main(string[] args) : var {
        Console.write("Hello, World!");
    }
}